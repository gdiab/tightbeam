## What this adds

The **Cursor harness**: Tightbeam can spawn, gate, and retire sessions running `cursor-agent` (ACP), alongside claude/codex. Built on a new **binary-native (shim) harness provisioning seam** shared with the held OpenCode adapter.

### Highlights

- **Shim seam** (`eb011e2`): provisioning for binary-native adapters launched through a verified shim.
- **Cursor adapter** (`cursor.ex` / `cursor_rails.ex`): rails compiled into Cursor's hooks gate (deny/allow with sentinels), fail-closed catalog, 0-LISTEN enforcement, readiness rendezvous.
- **Keychain-free daemon operation**: the vendor bundle honors `AGENT_CLI_CREDENTIAL_STORE=memory` + `CURSOR_API_KEY` env injection, so a LaunchDaemon-descended session never touches the user keychain (error-195 class). The bundle is **stock**, pinned by version `2026.08.11-e8db854` + sha256 of launcher and `index.js`; drift refuses rather than runs unverified bytes. This conforms to the uniform file-backed-cred + env-injection model — no vendored patch.
- **Model catalog wired from the pinned CLI** (`339edaa`, `77c59bf`), with the model-map gate fix (`aa24b25`).
- **Daemon-safe `lsof`** (`7c75df1` cherry-picked): the gateway LaunchDaemon's PATH lacks `/usr/sbin`; probes now use absolute paths with executable validation, failing closed without treating a PATH omission as evidence of no listener.
- **Held OpenCode scope extracted** (artifact `art_0672c627` on `wi_70de891b`): the three-file OpenCode harness module was removed from this PR's diff and preserved for upstream-fix landing. The shared binary-native shim seam remains for Cursor.

### Evidence

- Full suites at this head: **1694 Elixir passed** (11 skipped), **297 Rust passed**, 0 failures.
- Cursor harness coverage is unit/integration tested (registration, catalog intersection, rails wrapper shape, local-only refusal). **No recorded live Cursor smoke leg** at this head — see `docs/SMOKE.md` harness-parity matrix; live acceptance remains INCOMPLETE until a durable env-injection turn is recorded.
- Known gap found by dogfood: the running gateway predates `aa24b25`, so `composer-2.5` sessions fail `:model_unavailable` until upgraded to this build; `auto` works.

### Base note

CONTRIBUTING on `main` still carries the 2026-08-18 "0.1.9 is not a supported line" ruling; per Mike (2026-08-24), 0.1.9 is re-cut and PRs land there and are merged forward to 0.2.0. This PR targets `0.1.9` on that instruction.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01C7HAYzvEqEoEEVJQsgayx6
